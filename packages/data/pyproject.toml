[project]
name = "data"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "matthias springstein", email = "matthias.springstein@gmail.com" },
]
requires-python = ">=3.12"
dependencies = ["interface", "utils", "mava-exchange"]

[tool.uv]
package = true

[tool.uv.sources]
interface = { workspace = true }
utils = { workspace = true }
mava-exchange = { git = "https://github.com/sdsc-ordes/mava-exchange.git" }
